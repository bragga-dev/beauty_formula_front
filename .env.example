# URL base da API (sem barra final)
VITE_API_URL=http://localhost:8000/api



# Google OAuth (Google Identity Services) — mesmo client_id configurado no back-end
VITE_GOOGLE_CLIENT_ID=



# URL base da API (sem barra final)
# Local: http://localhost:8000/api
# Vercel (apontando pro back local via ngrok): configurar essa mesma
# variável em Project Settings -> Environment Variables na Vercel,
# não neste arquivo (este .env não vai no deploy).
VITE_API_URL=https://subhedral-bobette-loquaciously.ngrok-free.dev/api
